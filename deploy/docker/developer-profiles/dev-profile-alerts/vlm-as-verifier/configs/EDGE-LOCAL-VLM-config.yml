# SPDX-FileCopyrightText: Copyright (c) 2025-2026, NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# ============================================================================
# SIMPLIFIED ALERT AGENT CONFIGURATION
# ============================================================================
# This configuration supports the simplified alert processing flow:
# JSON Input → Entity Building → VIOS/VST Clip Resolution → VLM Verification → Enhanced JSON Output

# Base directory for media used during alert review (optional)
# If set, components can resolve relative media paths against this base dir
ALERT_REVIEW_MEDIA_BASE_DIR: ""

# VST Configuration
vst_config:
  recording_check_max_attempts: 3
  # Base URL for general VST APIs already used elsewhere in the codebase
  base_url: ${VST_INTERNAL_URL}
  sensor_list_endpoint: "/vst/api/v1/sensor/streams"
  add_overlay: false
  segment_anchor: "end"          # enable end-anchored window
  segment_duration_seconds: 10   # M (already used today)
  timeout: 60                    # timeout in seconds to get the video url from vst
  retry_without_overlay: true
  verify_time_existence: true

  # Storage service configuration (its own base URL and endpoint, separate from the VST APIs above)
  storage:
    base_url: ${VST_INTERNAL_URL}
    # Endpoint to fetch media file path by VST id
    media_file_path_by_id_endpoint: "/api/v1/storage/file/path"

# General Kafka Configuration - Message Processing
kafka:
  bootstrap_servers: "kafka:29092"
  group_id: 'kafka-incidents-dumper'
           # Output topic for incidents
  max_poll_records: 10                          # Process one record at a time (immediate processing)
  auto_offset_reset: 'latest'
  enable_auto_commit: false
  max_poll_interval_ms: 600000                  # Reduced to 5 minutes
  session_timeout_ms: 45000
  heartbeat_interval_ms: 15000
  poll_timeout: 5                             # Kafka consumer poll wait timeout in milliseconds
  message_type: "Incident"  # Protobuf message type: "Behavior" or "Incident"
  enhanced_anomaly_topic: "alert-bridge-enhanced-alerts"     # Not currently used will be removed in the next version
  incidents_topic: "alert-bridge-incidents" # Not currently used will be removed in the next version

#VLM Configuration
vlm:
  base_url: ${VLM_BASE_URL}/v1
  model: "${VLM_NAME}"
  max_tokens: 4096
  request_timeout: 5

  # VLM Media Source: how to send video to VLM endpoint
  # false (default) - Pass video URL to VLM (VLM fetches from URL)
  # true - Download video locally, base64-encode, upload to VLM
  vlm_media_source_using_base64: false

  # Response format: auto | cosmos-reason | json | other | <custom>  (default: auto)
  response_format: "auto"

  # Media Processing Parameters Toggle
  # When true: Alert Bridge omits these parameters, letting VLM use its server-side defaults
  use_vlm_media_defaults: false
  # Custom media processing parameters (only used when use_vlm_media_defaults: false)
  temperature: 0.6
  # Beware that these parameters need to be set in accordance with the VLM max context window
  min_pixels: 1568
  max_pixels: 3960000
  enable_sampling: true
  sampling_fps: 4

# Event Bridge Configuration - Kafka source/sink (Redis removed)
event_bridge:
  sourceType: "kafka"
  sinkType: "kafka"

  kafka_source:
    group_id: 'alert-bridge-vlm-group'
    topics:
      incident: 'mdx-incidents'
      alert: 'mdx-alerts'

prompt:
  prefer_payload_prompt: false
  override_prompts_on_start: true
  default_system_prompt: "You are a helpful assistant."

# Alert Type Configuration
alert_type_config_file: "alert_type_config.json"

# Alert Agent Configuration - Processing Settings
alert_agent:
  num_workers: 10                                # Number of worker threads
  max_allowed_stream_size: 2                    # Maximum stream size in minutes
  default_stream_interval: 1                    # Default stream interval in minutes
  vst_pass_through_mode: false                   # Use local media files instead of VST stream lookup

  # Dedup / filter tuning (in-process dedup + ES-backed verdict protection).
  event_filters:
    dedup_ttl_seconds: 5
    # Protect confirmed VLM verdicts from being re-verified (Elasticsearch-backed).
    protect_confirmed_verdicts:
      enabled: true
      ttl_seconds: 600
      # Verdict-ES retry cooldown + hourly throttled expired-marker reaper.
      es_retry_backoff_seconds: 30
      retention_interval_seconds: 3600
      retention_requests_per_second: 50
    # End time delta filter: blocks incidents unless end time changed significantly.
    end_time_delta_filter:
      enabled: true
      threshold_seconds: 3
      ttl_seconds: 3600
    # Categories listed here retain the incident end timestamp when building the
    # dedup key. Any category not listed omits the end timestamp.
    end_time_in_dedup_key_categories: []

  metrics:
    # Requires PROMETHEUS_METRICS_ENABLED=true (env var) to have any effect.
    # When false (default), the *_BY_SENSOR metric variants are never observed
    # (zero cardinality cost). When true, each distinct sensor ID mints ~12
    # additional Prometheus series per histogram — only enable for deployments
    # with a bounded sensor fleet and a known series budget (~10k per target).
    per_sensor_labels: false
  # Pipeline mode: sync | thread_bridge | event_loop (invalid value fails startup).
  # For A/B perf arms flip only this key; thread_bridge reproduces the legacy
  # async path, sync is the reference arm. See services/alert/README.md
  # "Pipeline modes & concurrency sizing".
  pipeline_mode: event_loop

  # Pipeline processes. One instance spreads its consume + dispatch stack
  # across this many OS processes, which is what lifts it past a single core.
  # Above 1 it requires pipeline_mode: event_loop and shared alert-config
  # storage, and it must not exceed the partitions on ANY ONE source topic --
  # each topic is assigned independently, so a topic with fewer partitions than
  # processes leaves the difference holding none of it. With N replicas the
  # constraint is replicas x processes <= partitions, per topic.
  processes: 1
  # The whole of startup shares this budget: topic metadata, prompt seeding,
  # VLM warmup and every pipeline joining its consumer group. Exceeding it
  # fails the container rather than leaving it half-started. Raise it where
  # the default cannot be met -- topics created by a Job that races this
  # deployment, or several processes building their stores at once. Must be at
  # least twice the 15s reserved for the group join. The hard kill lands a
  # 5s watchdog margin past the budget -- 65s at this default -- so size a
  # startup probe against that, not against the budget alone.
  startup_timeout_seconds: 60
  async_dispatch_max_in_flight: 16              # Global in-flight bound, 2-4x max_vlm_concurrent
  async_io:
    external_timeout_seconds: 30                 # Timeout for async-submitted VST/Elastic/dedup operations
    sink_warn_in_flight: 20                      # Warning threshold for in-flight async sink operations
    # event_loop per-service caps. max_vlm_concurrent MUST be set explicitly
    # (default falls back to async_dispatch_workers) and MUST stay at or below
    # the benchmarked VLM backend ceiling.
    max_vlm_concurrent: 4
    max_vst_concurrent: 8

  # URL Transformation - transforms internal IPs to external for VLM/UI access
  url_transform:
    enabled: true                                # Enable URL transformation (default: true)

  media_download:
    enabled: false                                # Enable direct media URL processing
    max_media_count: 5                           # Maximum number of media URLs per request
    timeout_seconds: 30                          # Download timeout per file
    max_size_mb: 50                              # Maximum file size to download
    allow_private_urls: true                     # Allow private/local URLs (development only, SSRF risk)
    use_verdict: false                           # VLM response mode: true=verdict-based, false=freestyle

  # Enrichment Processing Configuration
  enrichment:
    enabled: false                               # Master switch for enrichment processing

  # Always-on VLM alert rules — set via ALERT_AGENT_ALWAYS_ON
  # (false for verification/2d_cv, true for real-time/2d_vlm).
  always_on: ${ALERT_AGENT_ALWAYS_ON}
  # OpenTelemetry tracing. ALERT_ENABLE_OTEL_MONITORING is the on/off switch; this
  # block is the tuning, the same way metrics pairs with PROMETHEUS_METRICS_ENABLED.
  tracing:
    # Fraction of traces sampled, 0.0-1.0, applied with ParentBased so a sampled
    # caller keeps its whole trace. This controls export volume and backend
    # storage cost, not CPU: the sampler decides inside start_span, so an event
    # whose trace is dropped has already paid for building the attributes. A
    # measured run put 0.1 within ~1 point of 1.0 on throughput, so reaching for
    # this key to recover headroom under load will not find any - turn the
    # feature off instead.
    sampling_ratio: 0.1
    # When false, prompt text, VLM responses and video URLs never reach a span.
    # Alert data may contain PII, so enabling this is a decision about one
    # specific environment - which is why it has no env-var shortcut.
    include_content: false
    # Applies only when include_content is true; the suffix counts against it.
    # A second ceiling applies past this one: the deployment sets
    # OTEL_ATTRIBUTE_VALUE_LENGTH_LIMIT (4096), and the SDK cuts at that
    # without leaving the "+N chars" marker. Raise both together, or the
    # span will be trimmed with nothing saying it was.
    max_content_chars: 512

# RTVI VLM Configuration - Real-time VLM alert management
rtvi_vlm:
  base_url: ${RTVI_VLM_BASE_URL}/v1
  timeout: 10                                # HTTP request timeout (seconds)
  default_model: "${VLM_NAME}"  # Default VLM model for caption/alert generation
  rules_collection: "alert-realtime-rules"   # ES collection name for durable rule storage
  pending_ttl_seconds: 300                   # Stale pending rules older than this are cleaned at startup
  health_check_interval_seconds: 30          # RTVI /ready poll interval; triggers reconcile on recovery
  # Rollback switch for the realtime-rules persistence + replay feature.
  # When false, POST/DELETE on /api/v1/realtime do not write to
  # Elasticsearch and POST /api/v1/realtime/replay returns 501.
  # Independent of `persistence.enabled`: the global flag turns the whole
  # persistence layer (alert configs, prompts, realtime) off, while this
  # one only disables the realtime-rules path.
  enable_realtime_persistence: true

  # Realtime alert consolidation — group repeated VLM positives from the same
  # camera and alert type into a single event. Controlled per request via the
  # `consolidate` query param on GET /api/v1/realtime/incidents (omitted/false =
  # raw chunk-level documents; true = consolidated). The keys below only tune the grouping.
  consolidation:
    max_inter_alert_gap_seconds: 60    # a positive arriving after this gap starts a new event
    max_event_duration_seconds: 300    # cap on a single consolidated event (null = unbounded)
    representative: "latest"           # chunk that supplies the representative fields: latest | longest_reasoning

cors:
  enabled: true
  allow_origins: ['*']
  allow_methods: ['*']
  allow_headers: ['*']
  allow_credentials: false

elastic:
  enabled: true
  hosts:
    - http://elasticsearch:9200

# Persistence layer — durable storage for configs and prompts
persistence:
  enabled: true
  backend: "elasticsearch"               # Only "elasticsearch" supported for now
  index_prefix: "ab-"                    # Indices created as ab-{collection}
  # elasticsearch:                       # Optional: override hosts (defaults to elastic.hosts above)
  #   hosts:
  #     - http://elasticsearch:9200

vlm_enhanced_sink:
  incident:
    type: "elastic"
    elastic:
      index: "mdx-vlm-incidents"
  alert:
    type: "elastic"
    elastic:
      index: "mdx-vlm-alerts"
# vlm_enhanced_sink:
#   incident:
#     type: "kafka"
#     kafka:
#       topic: "mdx-vlm-incidents"
#       message_type: "incident"
#       key_field: "id"
#   alert:
#     type: "kafka"
#     kafka:
#       topic: "mdx-vlm-alerts"
#       message_type: "alert"
#       key_field: "id"

# vlm_enhanced_incident_sink:
#   type: "kafka"
#   kafka:
#     topic: "mdx-vlm-incidents"
#     message_type: "incident"
#     key_field: "id"

logging:
  level: "INFO"                                 # Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (applies to all components)
  format: "%(asctime)s - %(threadName)s - %(name)s - %(levelname)s - %(message)s"
  third_party_level: "WARNING"                  # Level for urllib3/httpcore/httpx/elasticsearch, etc.
  truncate_base64: true                         # Truncate base64 data URLs in logs to prevent long output (env: LOG_TRUNCATE_BASE64)