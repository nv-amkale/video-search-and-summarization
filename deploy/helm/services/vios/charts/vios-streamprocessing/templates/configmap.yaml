# SPDX-FileCopyrightText: Copyright (c) 2025-2026, NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vss-vios-streamprocessing.fullname" . }}-configs
  labels:
    app.kubernetes.io/name: vss-vios-streamprocessing
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
  vst_config.json: |
{{- $g := .Values.global | default dict }}
{{- $pfx := default false (coalesce .Values.useReleaseNamePrefix (index $g "useReleaseNamePrefix")) }}
{{- $redisH := ternary (printf "%s-redis" .Release.Name) "redis" $pfx }}
{{- $kafkaH := ternary (printf "%s-kafka-kafka" .Release.Name) "kafka-kafka" $pfx }}
{{- $esH := ternary (printf "%s-elasticsearch" .Release.Name) "elasticsearch" $pfx }}
{{- $redisAddr := .Values.redisServerAddress | default (printf "%s:6379" $redisH) }}
{{- $kafkaAddr := .Values.kafkaServerAddress | default (printf "%s:9092" $kafkaH) }}
{{- $rtviVlmH := ternary (printf "%s-vss-rtvi-vlm" .Release.Name) "vss-rtvi-vlm" $pfx }}
{{- $rtviVlmAddr := .Values.rtviVlmServerAddress | default (printf "%s:8000" $rtviVlmH) }}
{{- $viosGlobal := index $g "vios" | default dict }}
{{- $notificationConfig := .Values.notificationConfig | default (index $viosGlobal "notificationConfig" | default "") | default (.Files.Get "configs/notification_config.json") }}
{{- $messageBrokerConsumer := .Values.messageBrokerConsumer | default (index $viosGlobal "messageBrokerConsumer" | default "redis") }}
{{- $messageBrokerTopicConsumer := .Values.messageBrokerTopicConsumer | default (index $viosGlobal "messageBrokerTopicConsumer" | default "mdx-raw") }}
{{- $messageBrokerMetadataTopic := .Values.messageBrokerMetadataTopic | default (index $viosGlobal "messageBrokerMetadataTopic" | default "mdx-raw") }}
{{- $videoMetaIndexPattern := .Values.videoMetadataIndexPattern | default "mdx-raw*" }}
{{- $videoMeta := .Values.videoMetadataServerUrl | default (printf "%s:9200/%s" $esH $videoMetaIndexPattern) }}
{{- $cloudStorage := .Values.cloudStorageEndpoint | default "http://127.0.0.1:9000" }}
{{- $mqttHost := ternary (printf "%s-mosquitto" .Release.Name) "mosquitto" $pfx }}
{{- $mqttAddr := .Values.mqttBrokerAddress | default (printf "tcp://%s:1883" $mqttHost) }}
{{- $calibEp := .Values.calibrationFileEndpoint | default "" }}
{{- $floorEp := .Values.floormapImageEndpoint | default "" }}
{{- $overlaySensorName := .Values.overlaySensorName | default "" }}
{{- $extHost := index $g "externalHost" | default "" | toString | trim }}
{{- $extPort := index $g "externalPort" | default "" | toString | trim }}
{{- $extScheme := index $g "externalScheme" | default "http" }}
{{- $vssIngress := index $g "vssIngress" | default dict }}
{{- $ingressEnabled := index $vssIngress "enabled" | default false }}
{{- $analyticsAddr := .Values.analyticsServerAddress | default (ternary (ternary (printf "%s://%s:%s/video-analytics-api" $extScheme $extHost $extPort) (printf "%s://%s/video-analytics-api" $extScheme $extHost) (ne $extPort "")) "" (and (ne $extHost "") $ingressEnabled)) }}
{{- $softwarePath := .Values.useSoftwarePath | default false }}
{{- $turnServerUrl := .Values.turnServerUrl | default (index $g "turnServerUrl" | default "") }}
{{- $turnUrls := list }}
{{- if $turnServerUrl }}
{{- range splitList "," $turnServerUrl }}
{{- $turnUrls = append $turnUrls (trim .) }}
{{- end }}
{{- end }}
{{ .Files.Get "configs/vst_config.json" | replace "__VIDEO_METADATA_SERVER__" $videoMeta | replace "__CLOUD_STORAGE_ENDPOINT__" $cloudStorage | replace "__ANALYTICS_SERVER_ADDRESS__" $analyticsAddr | replace "__CALIBRATION_FILE_ENDPOINT__" $calibEp | replace "__FLOORMAP_IMAGE_ENDPOINT__" $floorEp | replace "__USE_SOFTWARE_PATH__" ($softwarePath | toString) | replace "__STATIC_TURNURL_LIST__" ($turnUrls | toJson) | replace "__3D_OVERLAY_SENSOR_NAME__" $overlaySensorName | indent 4 }}
  notification_config.json: |
{{ $notificationConfig | replace "__REDIS_ADDRESS__" $redisAddr | replace "__KAFKA_ADDRESS__" $kafkaAddr | replace "__MQTT_BROKER_ADDRESS__" $mqttAddr | replace "__RTVI_VLM_ADDRESS__" $rtviVlmAddr | replace "__USE_MESSAGE_BROKER_CONSUMER__" $messageBrokerConsumer | replace "__MESSAGE_BROKER_TOPIC_CONSUMER__" $messageBrokerTopicConsumer | replace "__MESSAGE_BROKER_METADATA_TOPIC__" $messageBrokerMetadataTopic | indent 4 }}
  adaptor_config.json: |
{{ .Files.Get "configs/adaptor_config.json" | indent 4 }}
  vst_storage.json: |
{{ .Files.Get "configs/vst_storage.json" | indent 4 }}
  rtsp_streams.json: |
{{ .Files.Get "configs/rtsp_streams.json" | indent 4 }}
{{- end }}
